let log = function(message){   //In JS we write the function declaration "function" to declare the function.
  console.log(message);
 }
 
 let llog= (message)=> console.log(message);  //In typescript we dont have to write the function name for functions such as these, using arrow functions will make it easier to work with.
